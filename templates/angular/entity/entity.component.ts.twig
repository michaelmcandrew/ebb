import { Component, OnInit, Input } from '@angular/core';
import { {{entity.names.upperCamel}} } from './{{entity.names.kebab}}.model';

@Component({
  selector: 'app-{{entity.names.kebab}}',
  templateUrl: './{{entity.names.kebab}}.component.html',
  styleUrls: ['./{{entity.names.kebab}}.component.css']
})

export class {{entity.names.upperCamel}}Component implements OnInit {
  @Input() {{entity.names.lowerCamel}} : {{entity.names.upperCamel}};
  ngOnInit() {
  }
}
